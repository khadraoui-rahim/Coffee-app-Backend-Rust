# Database Configuration
# Production database
DATABASE_URL=postgresql://coffee_user:coffee_pass@localhost:5432/coffee_db

# Test database (used during testing to avoid affecting production data)
# TEST_DATABASE_URL=postgresql://coffee_user:coffee_pass@localhost:5433/coffee_test_db

# Server Configuration
HOST=0.0.0.0
PORT=8080

# Logging
RUST_LOG=info

# Authentication
# JWT secret key for signing tokens (use a strong random string in production)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-here-change-in-production
